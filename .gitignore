# runtime state — never commit user accounts/sessions
server-data/

# local dev / working files not needed by the deployed site
.claude/
uploads/
screenshots/
design_handoff_matchupcoach/
tools/
.thumbnail
*.output
node_modules/
